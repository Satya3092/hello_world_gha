name: hello-world

on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo "demo build"

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: test phase
        run: echo "running test case"

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: deploy phase
        run: echo "this is deploy stage"
